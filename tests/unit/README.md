# Unit Tests

Fast, isolated tests for individual core computation modules.

Each module gets its own test file: `<module-name>.test.js`

Tests use JSON-LD fixtures as input and assert JSON-LD output matches expected results.
